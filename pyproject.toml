[project]
name = "vooc"
version = "0.3.0"
description = "Voice-Oriented Open Content Console"
requires-python = ">=3.11"
dependencies = [
  "ddgs>=9.0.0",
  "wikipedia>=1.4.0",
  "grokipedia-api>=0.1.0"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
